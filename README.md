# Ballistics Lab

Simple ballistic scenario simulation cli tool. Outputs matplotlib plots from cli arguments. Includes different solvers and drag models (vacuum, linear, quadratic).

**Example:**
```bash
python -m ballistics_lab.main --x0 0 --y0 100 --speed 15 --angle 0
```

**CLI Args:**
